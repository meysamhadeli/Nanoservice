apiVersion: v1
kind: Pod
metadata:
  name: nanoservice-pod
  labels:
    name: nanoservice
spec:
  containers:
  - name: nanoservice
    image: meysamh66/nanoservice
    imagePullPolicy: IfNotPresent
    ports:
    - containerPort: 5000

    